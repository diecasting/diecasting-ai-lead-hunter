"""Lead Discovery domain package (Phase 5 Stage 1).

Hosts the website-analysis pipeline that turns a prospect URL into a
qualification-ready discovery record (``analyzer``) plus the persistence
layer for ``CompanyDiscovery`` rows (``crud``).
"""
