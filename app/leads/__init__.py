"""Lead-domain business logic package (Phase 4 Stage 3.5).

Currently hosts the bulk CSV / Excel import engine (``importer``); future
lead-domain services (dedup, enrichment pipelines) should live here too.
"""
