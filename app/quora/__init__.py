"""Phase 7 — Quora + SEO Authority Engine.

Submodules:
* ``content_db``   — the curated industrial content database (store + search).
* ``discovery``    — discover Quora questions (injectable search resolver).
* ``answer_generator`` — ground answers in the content DB (AI + deterministic).
* ``workflow``     — explicit status state machines for questions / answers.
* ``export``       — Markdown export for answers and blog posts.
* ``seo``          — the SEO blog reuse pipeline (answer/content -> BlogPost).
"""
